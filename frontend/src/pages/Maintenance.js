import React from "react";

const MaintenancePage = () => (
  <div style={{
    minHeight: "100vh",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
    padding: "2rem"
  }}>
    <h1>🛠️ We'll be back soon!</h1>
    <p>Newstarn is currently undergoing scheduled maintenance.<br />Please check back shortly.</p>
  </div>
);

export default MaintenancePage;
